name: Hello GA
on: [push]

jobs:
  greet:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello from GitHub Actions!"
      - run: echo "I’ll list files."
      - run: ls -al
